---
name: fix-issue
description: Fix a bug, analyzer/lint error, build failure, runtime exception, or design-system deviation in a Flutter app generated by this framework. Use when the user reports something broken or failing. Targets the root cause, never a workaround.
model: sonnet
---

# /fix-issue — Fix a bug (root cause)

## Role
Debugger — find the root cause and fix it cleanly, never paper over it.

## Goal
Resolve the failure so the relevant `rules/verification.md` checks pass, with the minimum correct change.

## Deliverable
The corrected file(s) on disk + a passing verification + (after a multi-attempt anomaly) a cleanup report.

---

> If the project root has not been provided in this flow, first ask: `Racine du projet ? (chemin du dossier)`.

## Decision tree — classify the failure first

Read the actual error before touching code. Classify, then act in this order:

### 1. Analyzer / lint error (`flutter analyze`, `dart run custom_lint`)
- Read the exact message + `file:line`. Most are precise (missing import, type mismatch, dead code, unused, Riverpod lint).
- Fix the cause (correct the type, add the import, remove the unused symbol) — **do not** add `// ignore:` to silence it unless the lint is genuinely wrong and you justify it in a comment.

### 2. Compilation error (build fails before running)
- Often a missing `.g.dart` → run `dart run build_runner build --delete-conflicting-outputs` first; the generated file may simply be stale.
- Otherwise: signature mismatch, missing provider, broken import path. Trace to the declaration.

### 3. Runtime exception (app runs, then throws)
- Reproduce mentally from the stack trace. Identify the layer:
  - SQLite/`shared_preferences`/parsing → likely `data`; ensure the business exception is named and caught in `application`.
  - Null / state / `AsyncValue` misuse → likely `application` or the `.when()` in `presentation`.
  - `ProviderNotFound` / lifecycle → wiring in `main.dart` or a missing `ProviderScope`.
- Fix at the layer that owns the cause, not where the symptom surfaces.

### 4. Design-system / layout deviation (visual bug)
- Hardcoded color/size, missing dark-mode handling, raw `SnackBar`/`AlertDialog`, non-flat element.
- Route the value through `tokens.dart`/`context.colors`, or replace the native widget with the framework one (`AppDialog`, toast). See `rules/theme.md` / `rules/errors.md`.

## Steps

Read `design-system.md` / `layout.md` on demand if the fix touches UI (no longer auto-imported).

1. Get the real error (command output, stack trace, or the user's repro). If you cannot reproduce, ask for the exact message — do not guess.
2. Classify with the tree above; read both the failing site and the declaration it depends on.
3. **Ask: "knowing what I know now, what is the clean fix?"** Implement that, not the quickest patch. If the clean fix is larger than expected, say so before applying.
4. Apply the minimum correct change. Respect the layers and the contract (`docs/specs/04-architect.md`).
5. **Verify**: re-run `rules/verification.md §A` for the affected area; confirm the targeted failure is gone and nothing else broke.
6. If it took several attempts: produce the **cleanup report** (`rules/architecture.md` — list every dead element added during failed attempts), then offer `Veux-tu mémoriser ce point ? /save-memory`.

## Anti-patterns — what NOT to do
- **Do not** silence an analyzer/lint finding with `// ignore:` instead of fixing the cause (unless the lint is wrong and you say why).
- **Do not** wrap a bug in a `try/catch {}` that hides it — that is a symptom mask, not a fix.
- **Do not** hardcode a value to "make it look right" — fix it through the tokens.
- **Do not** leave the failed-attempt residue in the file. Clean it up.
- **Do not** expand the fix into an unrequested refactor; if the code around is genuinely wrong, flag it for `/refactor-code`.
- **Do not** claim it is fixed without re-running the verification (or telling the user which command to run if no SDK).

## When the user asks something adjacent
- **"Just make the error go away"** → the error is a symptom; explain you are fixing the cause so it does not resurface. If they truly want a temporary silence, do it explicitly with a justified `// ignore:` and flag it as debt.
- **"It's slow / janky"** → not a crash; this is an optimization (const, rebuilds, list lazy-loading) — handle as a `/phase5` finalization or `/refactor-code` item, on request.
